# Nixpacks configuration for Railway
# This helps Railway detect the Node.js project correctly

[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm ci --ignore-scripts --omit-dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

